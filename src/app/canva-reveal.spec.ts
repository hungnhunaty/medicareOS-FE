import { CanvaReveal } from './canva-reveal';

describe('CanvaReveal', () => {
  it('should create an instance', () => {
    const directive = new CanvaReveal();
    expect(directive).toBeTruthy();
  });
});
